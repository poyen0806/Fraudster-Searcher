/// Enum to represent the different modes or roles in the application.
enum Mode {
  /// Represents the admin mode or role in the application.
  /// admin / system -> user
  admin,

  /// Represents the user mode or role in the application.
  /// user -> admin / system
  user,
}
